# 模块 9：HTTP 管理端口与可观测性

管理端口复用 Reactor/TcpServer，提供 `/metrics`、`/status` 和 `/config`。Prometheus
指标覆盖命令、错误、连接、网络字节、命中、过期、淘汰和命令延迟直方图。

指标采用原子计数；状态接口通过 shard 任务汇总键数和近似内存，不直接跨线程读取存储。

完成度：模块 9/11，项目约 86%。
